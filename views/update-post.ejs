<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("includes/head.ejs") %>
    <link rel="stylesheet" href="/styles/create-posts.css" />
    <title>Update Post</title>
  </head>
  <body>
    <header id="main-header"><%- include("includes/header.ejs") %></header>
    <main>
      <h1>Add a new post</h1>
      <form action="/post/<%=post._id%>/edit" method="POST">
        <div class="form-control">
          <label for="title">Title</label>
          <input
            type="text"
            id="title"
            name="title"
            value="<%=post.title%>"
            required
          />
        </div>

        <div class="form-control">
          <label for="summary">Summary</label>
          <input
            type="text"
            id="summary"
            name="summary"
            minlength="10"
            value="<%=post.summary%>"
            required
          />
        </div>
        <div class="form-control">
          <label for="content">Post Content</label>
          <textarea
            name="content"
            id="content"
            cols="30"
            rows="10"
            minlength="20"
            required
          >
<%=post.body%></textarea
          >
        </div>
        <button type="submit" class="btn">Update Post</button>
      </form>
    </main>
  </body>
</html>
